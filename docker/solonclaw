#!/usr/bin/env bash
set -e

export SOLONCLAW_HOME="${SOLONCLAW_HOME:-/app/workspace}"
export SOLONCLAW_SERVER_URL="${SOLONCLAW_SERVER_URL:-http://127.0.0.1:8080}"

exec node /app/terminal-ui/entry.js "$@"
