# 项目全面修复上下文恢复记录

日期：2026-07-17

## 阶段 0.1 结论

本轮已读取 `/Users/chengliang/.codex/memories` 下除 `.git` 外的全部记忆文件，共 14,992 行，范围包括：

- `memory_summary.md`、`MEMORY.md`、`raw_memories.md`
- `.disk_rollout_refs`、`.mem_rollout_refs`
- `extensions/ad_hoc/` 下的说明和记录
- `rollout_summaries/` 下按文件名排序的全部 84 份摘要

核心记忆文件和摘要文件由独立只读代理分组逐行复核，主线程另外读取了本仓库相关索引、最近摘要和旧全面修复记录。所有代理仅提供证据，没有修改源码、配置或提交记录。

## 当前权威基线

- 本轮起点：`main` 的 `f274d8399`。
- 起点同时对应 `origin/main`、`gitee/main`，工作树无未提交内容。
- 本轮工作分支：`work/comprehensive-repair-20260717`。
- 旧工作分支 `work/full-repair-optimization` 的 `8603e529b` 已是当前 `main` 的祖先，旧版阶段修复已经进入当前主线，不重复搬运旧分支。
- 旧版 `docs/full-repair-*` 文档后来从主线清理，但对应实现和提交历史仍在；本轮以当前源码、测试和 Git 状态为准重新验收。

## 已恢复的有效上下文

1. 旧版全面修复已覆盖过阶段 1 至阶段 6 的多轮修复，包括大类拆分、未使用变量门禁、重复代码复用、前后端入口补齐、自然语言工具入口、数据显示刷新和错误态处理。
2. 2026-07-16 又完成了路由扁平化、规范文档单一来源、通知目标管理员绑定、兼容依赖升级及多项运行可靠性修复。
3. 旧结论存在明显时效性：文件行数、前后端接口、插件、多模态、语音、价格、Profile、测试基线和分支状态都已继续变化，必须按当前树重新扫描。
4. 当前仓库的真实验证顺序保持为：窄测试或静态门禁、单个 Maven 进程的 Java 验证、Web 构建、Terminal UI 类型检查和测试、命名与异常日志检查。
5. GitHub 连接失败时先检查本机代理；不得用重复重试替代根因排查。

## 本轮执行边界

- 严格按用户给定阶段顺序推进，每个子任务独立提交，提交信息以中文为主并附英文。
- 独立代理可以并行做只读审计；只有主线程修改源码、合成结论、验证和提交。
- 历史报告中的问题先按当前代码判定为“仍存在、已修复、已过时或证据不足”，不得按旧行号直接修改。
- 不恢复历史配置键、环境变量、命令或兼容层，只使用当前 `solonclaw` 命名。
- 不把安全规则、权限门控和确定性协议改成不稳定的模型判断；AI 驱动只用于适合软决策和内容分析的部分。
- 最终 Java 验证只运行一个 Maven 进程，避免多个进程争用 `target/` 产生假失败。

## 阶段 0.2 输入

当前主线保留两份 2026-07-16 审计建议：

- `docs/project-suggestions-2026-07-16.md`
- `docs/personal-assistant-optimization-2026-07-16.md`

两份文档都明确包含超时推断，且多项结论已经被后续提交覆盖。阶段 0.2 将结合当前代码和独立审计，形成新的四态预存问题清单，只把仍可复现的问题纳入后续阶段。

## 阶段 0.1 验证证据

- 记忆文件清单与逐文件行数已复核，合计 14,992 行。
- `git merge-base --is-ancestor 8603e529b f274d8399` 验证旧全面修复提交已进入当前基线。
- `git status --short --branch` 在创建本记录前确认起点工作树干净。
